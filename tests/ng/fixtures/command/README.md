Task11 native matrix: a literal command with spaces, quotes, metacharacters,
custom exit, and signal.  Task10 must neither shell-expand tokens nor execute
the command; it marks the profile exclusive and warns that inner parallelism is
unbounded.
