# Probe fixtures

This directory contains only local, deterministic ownership fixtures. The
current T12b evidence leaves Q-PY-PROBE and Q-VT-PROBE unsupported, so T12c
must not execute these fixtures or claim serial/parallel observations until a
native worker-identity qualification is available.
