Task11 native matrix: `--lib`, `--bins`, and `--test NAME` standard targets;
doctest and custom-harness refusal; missing `cargo`; nonzero exit; and signal.
Task10 requires an explicit standard target so it never silently drops doctests.
