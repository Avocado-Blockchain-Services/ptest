# Build the Spot capacity controller image.
steps:
  - name: gcr.io/cloud-builders/docker
    args: ["build", "-f", "Dockerfile.spot-controller", "-t", "${_IMAGE}", "."]
images:
  - "${_IMAGE}"
timeout: 1800s
