# Repository history

The pre-NG implementation is retained only in Git at
`legacy/pre-ng-2026-09-17` (`bae8863`). It is not a runtime input, installer
fallback, migration procedure, or shipped compatibility layer.
