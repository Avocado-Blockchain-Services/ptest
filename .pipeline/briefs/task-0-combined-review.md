# Opus focused combined Task0 re-review

Read-only, no delegation, shell/test runs, edits or installs. Read audit-spec and
the common task review contract completely, then the original task0 review brief.
The launch wrapper supplies exact combined HEAD and original base042dde1, combined
review package, repair-only diff, root verification and both task reports.

Opus's original verdict is .pipeline/out/task-0-review-opus.json in chain. Resolve
each T0-1..T0-7 against actual repaired bytes/evidence. Current approved design
includes Astra's public-summary addendum2162cf2; this clarifies true omissions,
not an unapproved implementation design change. Inspect that narrow addendum as
part of accepting the shared barrier. No downstream consumer has started.

Focus especially on:
- Mac Application Support and ordinary spaces/Unicode paths, no-follow structural
  safety, successful/failed nested FD closure, SQLite cleanup and escaped URI.
- Production RunResult serialization uses result.mode; private input snapshots,
  sequences/policy/internal fields and raw argv/env do not reach public documents.
- ConfigSummary/EffectiveLimits/InitAction/history reuse exactly match addendum.
  All named nested public objects are typed through one descriptor authority.
  Producers emit allowlists; public decoders validate required known fields then
  recursively DROP additive unknowns. Public schemas permit compatible additions;
  private guard protocols remain strict. No unsafe raw config projection islands.
- Deep JSON parsing and private constructor errors return bounded redacted errors.
- Shared invoke helper has required explicit finite positive timeout, captures
  bounded bytes DURING reading, reads export boundedly, handles both streams and
  owned retained-pipe children without hanging or touching unrelated jobs.
  Generated wrapper options precede native tail; explicit result option is
  recognized only in wrapper prefix; native argv bytes including -- and option-
  looking values are preserved. Check a real miniature prefix-only parser test,
  not permissive argparse masking the bug. T11 owns actual ptest.__main__.py;
  test-only tiny targets do not prove real native CLI integration yet.

Review new regressions for real assertions, raw retained RED/GREEN and duration
outliers, not report checkmarks. Root-directed Muse restarts are not failing tests;
prior scratch-write deviation is disclosed, not erased. Source ownership combines
original Task0 and disjoint helper repair plus author-owned docs, not broad scope.
Return supplied JSON with per-original-finding disposition in notes, fresh required
findings if any, truthful checked/not-checked coverage. No full suite at this gate.
